<ui:FluentWindow x:Class="Sifra.Desktop.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Options" Height="300" Width="460" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="None"
        Background="{DynamicResource Sifra.WindowBrush}">
    <Grid Background="{DynamicResource Sifra.WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Options" Background="{DynamicResource Sifra.WindowBrush}"
                     ShowMaximize="False" ShowMinimize="False"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <ui:TextBlock Text="Auto-lock" FontTypography="BodyStrong" Margin="0,0,0,6"/>
            <ui:TextBlock Text="Lock the vault automatically after this much inactivity."
                          FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,10" TextWrapping="Wrap"/>
            <ComboBox x:Name="AutoLockCombo" HorizontalAlignment="Left" Width="200" Margin="0,0,0,20"/>

            <ui:TextBlock Text="Browser extensions" FontTypography="BodyStrong" Margin="0,0,0,6"/>
            <ui:TextBlock Text="Manage which browsers can access this vault through the Sifra extension."
                          FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,10" TextWrapping="Wrap"/>
            <ui:Button Content="Manage Extensions..." HorizontalAlignment="Left" Click="OnManageExtensionsClick"/>
        </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <ui:Button Content="Save" Appearance="Primary" Margin="0,0,10,0" Click="OnSaveClick"/>
            <ui:Button Content="Cancel" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
