<ui:FluentWindow x:Class="Sifra.Desktop.GeneratorOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Password Options" Height="460" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="None"
        Background="{DynamicResource Sifra.WindowBrush}">
    <Grid Background="{DynamicResource Sifra.WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Password Options" Background="{DynamicResource Sifra.WindowBrush}"
                     ShowMaximize="False" ShowMinimize="False"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <Border Background="{DynamicResource Sifra.CardBrush}" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,16">
                <StackPanel>
                    <ui:TextBlock Text="Memorable" FontTypography="BodyStrong" Margin="0,0,0,10"/>
                    <ui:TextBlock Text="Separator" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ui:TextBox x:Name="SeparatorBox" MaxLength="3"/>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource Sifra.CardBrush}" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="14">
                <StackPanel>
                    <ui:TextBlock Text="Random" FontTypography="BodyStrong" Margin="0,0,0,10"/>
                    <ui:TextBlock Text="Symbols" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ui:TextBox x:Name="SymbolsBox" TextWrapping="Wrap" Margin="0,0,0,12"/>
                    <CheckBox x:Name="ExcludeSimilarCheckBox" Content="Exclude similar characters: 1-l-I, 0-O"/>
                </StackPanel>
            </Border>
        </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <ui:Button Content="Restore defaults" Margin="0,0,10,0" Click="OnRestoreClick"/>
            <ui:Button Content="Close" Appearance="Primary" Click="OnCloseClick"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
