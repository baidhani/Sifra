<UserControl x:Class="Sifra.Desktop.CredentialDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Sifra.Desktop"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="420">
    <Grid>
        <!-- Empty state -->
        <ui:TextBlock x:Name="EmptyStateText" Text="Select a credential to view its details."
                      FontTypography="Body" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ScrollViewer x:Name="DetailScroll" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="AvatarBorder" Grid.Column="0" Width="44" Height="44" CornerRadius="10"
                            Background="{DynamicResource Sifra.Brand.PrimaryBrush}" Margin="0,0,14,0"
                            Cursor="Hand" ToolTip="Change icon" MouseLeftButtonUp="OnAvatarClick">
                        <Border.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Use website icon" Click="OnUseWebsiteIconClick"/>
                                <MenuItem Header="Select symbol" Click="OnSelectSymbolClick"/>
                                <MenuItem Header="Select color" Click="OnSelectColorClick"/>
                                <MenuItem Header="Use custom icon" Click="OnUseCustomIconClick"/>
                            </ContextMenu>
                        </Border.ContextMenu>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <ui:TextBlock x:Name="TitleText" FontTypography="Subtitle" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                        <ui:TextBlock x:Name="HealthText" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    </StackPanel>
                    <ui:Button Grid.Column="2" x:Name="FavoriteButton" Icon="{ui:SymbolIcon Star24}" Click="OnFavoriteClick" ToolTip="Toggle favorite"/>
                </Grid>

                <Border x:Name="TagsPanel" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,14" Margin="0,0,0,14">
                    <StackPanel>
                        <ui:TextBlock Text="Tags" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <ItemsControl x:Name="TagsList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:TagChipViewModel}">
                                    <Border Background="{DynamicResource Sifra.SelectedBrush}" CornerRadius="10" Padding="8,4" Margin="0,0,6,6">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:SymbolIcon Symbol="Tag24" Filled="True" FontSize="12" Foreground="{Binding ColorBrush}" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding Name}" FontSize="12" Foreground="{DynamicResource Sifra.SelectedTextBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Every field (login, password, website, or anything else)
                     renders here uniformly — built in code-behind since each
                     field type needs different interactive behavior (masked
                     reveal, live OTP code, clickable link, copy). Each row
                     brings its own bottom-border divider (see BuildFieldRow),
                     so no extra margin is needed here. -->
                <ItemsControl x:Name="FieldsList"/>

                <Border x:Name="NotesPanel" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,14" Margin="0,0,0,14">
                    <StackPanel>
                        <ui:TextBlock Text="Notes" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                        <ui:TextBlock x:Name="NotesText" FontTypography="Body" TextWrapping="Wrap" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                    </StackPanel>
                </Border>

                <Border x:Name="ImagesPanel" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,14" Margin="0,0,0,14">
                    <StackPanel>
                        <ui:TextBlock Text="Images" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <ItemsControl x:Name="ImagesList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border x:Name="FilesPanel" BorderBrush="{DynamicResource Sifra.BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,14" Margin="0,0,0,14">
                    <StackPanel>
                        <ui:TextBlock Text="Files" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <ItemsControl x:Name="FilesList"/>
                    </StackPanel>
                </Border>

                <ui:TextBlock x:Name="UpdatedText" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" HorizontalAlignment="Center"/>
                <ui:TextBlock x:Name="CreatedText" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <ui:Button Content="Edit" Icon="{ui:SymbolIcon Edit24}" Click="OnEditClick" HorizontalAlignment="Left"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
