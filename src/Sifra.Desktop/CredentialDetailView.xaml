<UserControl x:Class="Sifra.Desktop.CredentialDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="420">
    <Grid>
        <!-- Empty state -->
        <ui:TextBlock x:Name="EmptyStateText" Text="Select a credential to view its details."
                      FontTypography="Body" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ScrollViewer x:Name="DetailScroll" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="{DynamicResource Sifra.Brand.PrimaryBrush}" Margin="0,0,14,0">
                        <TextBlock x:Name="AvatarText" Foreground="White" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <ui:TextBlock x:Name="TitleText" FontTypography="Subtitle" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                        <ui:TextBlock x:Name="HealthText" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    </StackPanel>
                    <ui:Button Grid.Column="2" x:Name="FavoriteButton" Icon="{ui:SymbolIcon Star24}" Click="OnFavoriteClick" ToolTip="Toggle favorite"/>
                </Grid>

                <StackPanel x:Name="UsernamePanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Username" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock Grid.Column="0" x:Name="UsernameText" FontTypography="Body" Foreground="{DynamicResource Sifra.TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Copy24}" Click="OnCopyUsernameClick" ToolTip="Copy username"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="PasswordPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Password" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock Grid.Column="0" x:Name="PasswordText" FontTypography="Body" FontFamily="Consolas" Foreground="{DynamicResource Sifra.TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <ui:Button Grid.Column="1" x:Name="RevealPasswordButton" Icon="{ui:SymbolIcon Eye24}" Click="OnRevealPasswordClick" ToolTip="Show/hide password"/>
                        <ui:Button Grid.Column="2" Icon="{ui:SymbolIcon Copy24}" Click="OnCopyPasswordClick" ToolTip="Copy password"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="UrlPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Website" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <ui:HyperlinkButton x:Name="UrlLink" Click="OnOpenUrlClick"/>
                </StackPanel>

                <StackPanel x:Name="PhonePanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Phone" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <ui:TextBlock x:Name="PhoneText" FontTypography="Body" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                </StackPanel>

                <StackPanel x:Name="AccountNumberPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Account number" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <ui:TextBlock x:Name="AccountNumberText" FontTypography="Body" FontFamily="Consolas" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                </StackPanel>

                <StackPanel x:Name="PinPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="PIN" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock Grid.Column="0" x:Name="PinText" FontTypography="Body" FontFamily="Consolas" Foreground="{DynamicResource Sifra.TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <ui:Button Grid.Column="1" x:Name="RevealPinButton" Icon="{ui:SymbolIcon Eye24}" Click="OnRevealPinClick" ToolTip="Show/hide PIN"/>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="NotesPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Notes" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}"/>
                    <ui:TextBlock x:Name="NotesText" FontTypography="Body" TextWrapping="Wrap" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"/>
                </StackPanel>

                <StackPanel x:Name="LabelsPanel" Margin="0,0,0,14">
                    <ui:TextBlock Text="Labels" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <ItemsControl x:Name="LabelsList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Sifra.SelectedBrush}" CornerRadius="10" Padding="10,4" Margin="0,0,6,6">
                                    <TextBlock Text="{Binding}" FontSize="12" Foreground="{DynamicResource Sifra.SelectedTextBrush}"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <ui:TextBlock Text="Custom fields" FontTypography="BodyStrong" Foreground="{DynamicResource Sifra.TextPrimaryBrush}"
                              Margin="0,6,0,10" x:Name="CustomFieldsHeader"/>
                <ItemsControl x:Name="CustomFieldsList" Margin="0,0,0,14"/>

                <ui:TextBlock x:Name="UpdatedText" FontTypography="Caption" Foreground="{DynamicResource Sifra.TextSecondaryBrush}" Margin="0,0,0,16"/>

                <ui:Button Content="Edit" Icon="{ui:SymbolIcon Edit24}" Click="OnEditClick" HorizontalAlignment="Left"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
